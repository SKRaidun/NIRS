from pathlib import Path
from fileinput import filename

path_file = Path('.')

def read_sort_file():

    return 0
#print(list(path_file.glob('**/*.txt')))

# for file in files:
#     fname = os.path.basename(file)
#     dict_[fname] = (pd.read_csv(file, header=0, dtype=str, encoding='cp1252')
#                       .fillna(''))